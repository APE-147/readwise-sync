# Example environment for reader-sync (do not commit real values)

# Required: Readwise Reader API token
READWISE_TOKEN=your_readwise_token_here

# Optional overrides to avoid committing machine-specific paths
# RW_SYNC_WATCH_DIR=/absolute/or/relative/path/to/inbox
# RW_SYNC_DB_PATH=./data/state/rw_sync.db

# Optional: default conda env directory for rw_sync_push_new.sh when using the alias "System"
# RW_DEFAULT_ENV_DIR=/path/to/conda/envs/System

# Optional: name or path to conda env for rw_sync_push_new.sh
# RW_CONDA_ENV=readwise
